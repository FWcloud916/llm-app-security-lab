define bot day24 blocked
  "Blocked by the Day 24 application rail."

define flow day24 deterministic input
  $allowed = execute day24_guard(surface="input", text=$user_message)
  if not $allowed
    bot day24 blocked
    stop
